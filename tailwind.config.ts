import type {Config} from 'tailwindcss'

const config: Config = {
    content: [
        "./index.html",
        "./src/**/*.{ts,js,tsx,jsx}"
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
export default config
